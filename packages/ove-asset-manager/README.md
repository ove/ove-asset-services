To run this Service you must add Object store credentials into the Appsettings.json file as follows

  "s3Client": {
    "AccessKey": "secret",
    "Secret": "secret",
    "ServiceURL": "host"
  },